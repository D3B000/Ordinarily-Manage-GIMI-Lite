# ---------------------------------------------------------------------------
# 二进制标记：禁止 git 做换行转换与文本 diff。
# 本仓库随包分发 ritual 编译产物（.pyd/.dll/.exe），必须按二进制处理，
# 否则换行转换会直接损坏这些文件。
# ---------------------------------------------------------------------------
*.pyd    binary
*.dll    binary
*.exe    binary
*.ico    binary
*.png    binary
*.jpg    binary
*.jpeg   binary
*.webp   binary

# 注意：此处刻意不设置 `* text=auto eol=crlf`。
# 强制统一行尾会触发全仓库重整，产生一个覆盖所有文本文件的巨型 diff；
# 本项目为 Windows 桌面应用，当前按「提交什么就是什么」处理即可。
